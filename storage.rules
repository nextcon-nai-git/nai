rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Libera a pasta orcamentos e qualquer outro arquivo temporariamente para o fluxo comercial
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}