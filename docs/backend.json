
{
  "entities": {
    "UserProfile": {
      "title": "Perfil de Usuário",
      "type": "object",
      "description": "Dados do usuário no sistema, definindo seu nível de acesso.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { 
          "type": "string",
          "enum": ["SUPER_ADMIN", "CLIENT_ADMIN", "EMPLOYEE"],
          "description": "Papel do usuário: SUPER_ADMIN (Nextcon), CLIENT_ADMIN (Gestor Empresa), EMPLOYEE (Funcionário)."
        },
        "companyId": { "type": "string", "description": "Referência para a empresa do cliente." },
        "linkedEmployeeId": { "type": "string", "description": "Referência para o cadastro de funcionário se for EMPLOYEE." },
        "name": { "type": "string" }
      },
      "required": ["id", "role"]
    },
    "Company": {
      "title": "Empresa Cliente",
      "type": "object",
      "description": "Empresa atendida pela plataforma NAI.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "cnpj": { "type": "string" },
        "status": { "type": "string", "enum": ["ACTIVE", "BLOCKED"] },
        "subscriptionPlan": { "type": "string" },
        "settings": { "type": "object" }
      },
      "required": ["id", "name", "cnpj"]
    },
    "Employee": {
      "title": "Colaborador",
      "type": "object",
      "description": "Cadastro de vida vinculada a uma empresa.",
      "properties": {
        "id": { "type": "string" },
        "companyId": { "type": "string" },
        "name": { "type": "string" },
        "cpf": { "type": "string" },
        "registrationNumber": { "type": "string" },
        "status": { "type": "string" }
      },
      "required": ["id", "name", "companyId"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Tabela central de permissões de usuários."
    },
    "/companies/{companyId}": {
      "schema": "Company",
      "description": "Dados mestre das empresas clientes."
    },
    "/employees/{employeeId}": {
      "schema": "Employee",
      "description": "Base global de vidas gerenciadas."
    }
  }
}
