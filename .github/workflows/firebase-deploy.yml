name: Deploy to Firebase (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: echo "This workflow is kept for manual fallback only. Use .github/workflows/deploy.yml instead."
